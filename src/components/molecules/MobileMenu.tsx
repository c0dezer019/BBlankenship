import { FC, ReactElement } from 'react';
import MobileMenuIcon from '../atoms/StyledMobileMenuIcon';

// Intend to include the navbar here
const MobileMenu: FC = (): ReactElement => <MobileMenuIcon />

export default MobileMenu;
