import React, { FC, ReactElement } from 'react';

import IconContainer from '../atoms/IconContainer';

const ContactSideBar: FC = (): ReactElement => <IconContainer></IconContainer>;

export default ContactSideBar;
